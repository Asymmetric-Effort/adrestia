import "reflect-metadata";
import DbDelete from "./DbDelete";

export default class Database extends DbDelete {
    /*
     * This is the front end of the Database classes for use by
     * the application.
     */
    public constructor() {
        super();
    }
}
